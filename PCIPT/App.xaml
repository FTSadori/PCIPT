<Application x:Class="PCIPT.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PCIPT"
             StartupUri="Windows/AuthorizationWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="White"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FF2574A1"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF063F68"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FF012B41"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF00121F"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="RoundedButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="BorderThickness" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="VeryRoundedButton" BasedOn="{StaticResource RoundedButton}" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="100" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                                <Setter Property="Foreground" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <LinearGradientBrush x:Key="PrettyBlueGradient" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="#FF0F0062"/>
            <GradientStop Color="#FF0F0062" Offset="1"/>
            <GradientStop Color="#FF1A318F" Offset="0.528"/>
        </LinearGradientBrush>

        <Style x:Key="PrettyComboBox" TargetType="{x:Type ComboBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="5 7 0 0"/>
                            <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <ToggleButton Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" >
                                <ToggleButton.Template>
                                    <ControlTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="10*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Background="White" Grid.Column="1"></TextBlock>
                                            <Border x:Name="Border"  Grid.ColumnSpan="2" CornerRadius="5" Background="Transparent" BorderBrush="{StaticResource PrettyBlueGradient}" BorderThickness="3" />
                                            <Border Grid.Column="0" CornerRadius="5,0,0,5"  Margin="1"  Background="Transparent"  BorderBrush="{StaticResource PrettyBlueGradient}" BorderThickness="0,0,1,0" />
                                            <Path x:Name="Arrow" Grid.Column="1"  Fill="#FF0F0062" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True"  Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="AliceBlue" BorderBrush="{StaticResource PrettyBlueGradient}" BorderThickness="3" CornerRadius="3"/>
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
            </Style.Triggers>
        </Style>


        <Style x:Key="Background">
            <Style.Setters>
                <Setter Property="TextBlock.Opacity" Value="0"/>
                <Setter Property="Panel.ZIndex" Value="5"/>
                <Setter Property="Grid.ColumnSpan" Value="3"/>
                <Setter Property="Grid.RowSpan" Value="4"/>
                <Setter Property="TextBlock.Foreground" Value="{DynamicResource RoundedTextBoxGrad}"/>
            </Style.Setters>
        </Style>

        <Style x:Key="PrettyText" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
                <Setter Property="TextBlock.FontFamily" Value="Century Gothic"/>
                <Setter Property="TextBlock.FontSize" Value="32"/>
                <Setter Property="TextBlock.Foreground" Value="{DynamicResource RoundedTextBoxGrad}"/>
            </Style.Setters>
        </Style>

        <Style x:Key="SmallPrettyText" BasedOn="{StaticResource PrettyText}" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="TextBlock.FontSize" Value="16"/>
            </Style.Setters>
        </Style>

        <Style x:Key="LeftText" BasedOn="{StaticResource SmallPrettyText}" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="TextBlock.HorizontalAlignment" Value="Left"/>
                <Setter Property="TextBlock.Margin" Value="20 0 0 0"/>
            </Style.Setters>
        </Style>

        <Style x:Key="RightText" BasedOn="{StaticResource SmallPrettyText}" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="TextBlock.HorizontalAlignment" Value="Right"/>
                <Setter Property="TextBlock.Margin" Value="0 0 20 0"/>
            </Style.Setters>
        </Style>


        <LinearGradientBrush x:Key="RoundedTextBoxGrad" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="#FF0F0062"/>
            <GradientStop Color="#FF0F0062" Offset="1"/>
            <GradientStop Color="#FF1A318F" Offset="0.528"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DisableGradient" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="#992F4F4F"/>
            <GradientStop Color="#992F4F4F" Offset="1"/>
            <GradientStop Color="#99808080" Offset="0.528"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ErrorGradient" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="DarkRed"/>
            <GradientStop Color="DarkRed" Offset="1"/>
            <GradientStop Color="#FFD20000" Offset="0.528"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="SuccessGradient" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="DarkGreen"/>
            <GradientStop Color="DarkGreen" Offset="1"/>
            <GradientStop Color="Green" Offset="0.528"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BackgroundBrush" EndPoint="0,0.5" StartPoint="1,0.5">
            <GradientStop Color="#FF1F0037" Offset="0"/>
            <GradientStop Color="#FF1F0037" Offset="1"/>
            <GradientStop Color="#FF0F0050" Offset="0.2"/>
            <GradientStop Color="#FF0F0050" Offset="0.8"/>
        </LinearGradientBrush>

        <Style x:Key="RegButton" TargetType="Button" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Background" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="BorderBrush" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="FontFamily" Value="Century Gothic"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Margin" Value="7 2 7 2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="3"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="TextBlock.FontFamily" Value="Century Gothic"/>
            <Setter Property="TextBlock.FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="2 3 0 0"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="TextBlock.FontFamily" Value="Century Gothic"/>
            <Setter Property="TextBlock.FontSize" Value="18"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="2 3 0 0"/>
        </Style>

        <Style x:Key="PrettyBorder" TargetType="Border">
            <Setter Property="Margin" Value="7 2 7 2"/>
            <Setter Property="BorderBrush" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>

        <Style x:Key="PrettyCheckbox" TargetType="CheckBox">
            <Setter Property="Margin" Value="7 2 7 2"/>
            <Setter Property="BorderBrush" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="BorderThickness" Value="3"/>

            <Setter Property="Foreground" Value="{StaticResource RoundedTextBoxGrad}"/>
            <Setter Property="TextBlock.FontFamily" Value="Century Gothic"/>
            <Setter Property="TextBlock.FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2 3 0 0"/>
        </Style>

    </Application.Resources>
</Application>
